#!/usr/bin/env bash
# ptt - entry point. The keybind and the PATH link point here, the work lives
# under src/.
set -Eeuo pipefail

PTT_ROOT="$(dirname "$(readlink -f "$0")")"

exec "$PTT_ROOT/src/ptt" "$@"
